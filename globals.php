<?php
  global $dbname;
  global $gAccess;
  global $gAction;
  global $gCAL;
  global $gCalendar;
  global $gDBA;
  global $gDb;
  global $gDebug;
  global $gDraftMode;
  global $gFrom;
  global $gHost;
  global $gLF;
  global $gMailServer;
  global $gMemberId;
  global $gNumRows;
  global $gPoolId;
  global $gPost;
  global $gPwd;
  global $gServer;
  global $gSitePrefix;
  global $gSourceCode;
  global $gTeamId;
  global $gTeamName;
  global $gTrace;
  global $mysql_host;
  global $mysql_pass;
  global $mysql_user;
  global $result;
?>